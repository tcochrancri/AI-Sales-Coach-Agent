{
  "name": "managed-chatkit",
  "private": true,
  "scripts": {
    "dev": "node ./scripts/dev.mjs",
    "frontend": "npm.cmd --prefix frontend install && npm.cmd --prefix frontend run dev",
    "frontend:build": "npm.cmd --prefix frontend install && npm.cmd --prefix frontend run build",
    "frontend:lint": "npm.cmd --prefix frontend install && npm.cmd --prefix frontend run lint",
    "backend": "node ./backend/scripts/run.mjs"
  }
}

